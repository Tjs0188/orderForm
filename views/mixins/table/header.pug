mixin tableHeader(columns, actions)
  thead
    tr
      each col in columns
        th(scope="col" class=`px-6 py-3 text-start text-xs font-medium text-gray-500 uppercase ${col.class || ''}`)
          = col.name
      if actions
        th(scope="col" class=`px-6 py-3 text-start text-xs font-medium text-gray-500 uppercase`)
          | Actions